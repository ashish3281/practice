const second=require('./second');
console.log(second.y);
console.log(second.x);
console.log(second.z());
//export let x=10;
//export let y=12;
//that code does not run in node js
//but it can run in js